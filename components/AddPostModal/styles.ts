import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 10,
  },
  input: {
    backgroundColor: '#FFFFFF',
    marginBottom: 20,
    marginTop: 10,
    height: 150,
  },
});
